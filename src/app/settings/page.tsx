'use client';

import { Title, Text, Stack } from '@mantine/core';

export default function SettingsPage() {
  // TODO: Add settings state, handlers, and UI components here
  // Example: API keys, default models, preferences

  return (
    <Stack>
      <Title order={2}>Settings</Title>
      <Text c="dimmed">Manage your application settings and API keys.</Text>
      {/* Placeholder for settings UI */}
      <Text>Settings options will be implemented here.</Text>
    </Stack>
  );
} 